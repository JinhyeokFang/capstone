spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.MySQLDialect
    properties:
      hibernate:
        format_sql: true
    show-sql: true

  data:
    redis:
      host: localhost
      port: 6379
      database: 0

jwt:
  secret-key: test-secret-key-for-testing-purposes-only-minimum-256-bits-required-for-hmac-sha-algorithm
  access-token-expiration-millis: 600000
  refresh-token-expiration-millis: 604800000
